let dd : int = 2.0;

function pow( x : real, n : int ) : real
{
    let y : real = 4.0;
    if (n>0){
        while (n>0){
            set y <- y * x;
            set n <- n - 1;
        }
    }else{
            while (n<0){
                set y <- y / x;
                set n <- n + 1;
            }
    }
    y;
}

let a : int  = 1;

let a: real = 1.0;

set a <- pow(1.9,1);